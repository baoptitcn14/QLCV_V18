import { SearchOrgUserPipe } from './search-org-user.pipe';

describe('SearchOrgUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrgUserPipe();
    expect(pipe).toBeTruthy();
  });
});
